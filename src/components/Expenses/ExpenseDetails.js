const ExpenseDetails = (props) => {
    const title = props.title

    return (
        <div>
            <div>
            
                {title}
            
            </div>

        </div>
    );
}
export default ExpenseDetails;